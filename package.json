{
	"name": "overrule",
	"version": "0.7.4",
	"private": true,
	"description": "dev-only: the version changesets bumps, plus the cross end-to-end test and the --css oracle resolve tailwindcss from here when the scanned project has none",
	"scripts": {
		"check:runtime": "tsc -p tsconfig.json",
		"test": "bun test",
		"changeset:version": "changeset version && node scripts/sync-version.mjs && git add --all"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^1.0.0",
		"@changesets/cli": "^3.0.1",
		"@tailwindcss/node": "^4.3.0",
		"@types/bun": "^1.3.14",
		"eslint": "^9.30.0",
		"tailwindcss": "^4.3.0",
		"typescript": "^6.0.3"
	}
}
