{
	"name": "overrule-rs-dev",
	"private": true,
	"description": "dev-only: the cross end-to-end test and the --css oracle resolve tailwindcss from here when the scanned project has none",
	"scripts": {
		"build:runtime": "tsc -p tsconfig.runtime.json",
		"check:runtime": "tsc -p tsconfig.test.json",
		"test": "bun test"
	},
	"devDependencies": {
		"@tailwindcss/node": "^4.3.0",
		"@types/bun": "^1.3.14",
		"tailwindcss": "^4.3.0",
		"typescript": "^6.0.3"
	}
}
